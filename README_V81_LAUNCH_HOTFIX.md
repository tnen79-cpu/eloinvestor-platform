# v81 Launch Hotfix

- Fixed admin modal/header z-index overlap.
- Compacted admin project editor action buttons.
- Added root route support for `/project/...` and `/auth/...` redirects to `/om/ar/...`.
- Added `.npmrc` forcing public npm registry.
- Added `vercel.json` with install command using `registry.npmjs.org`.

## Vercel
If deploy still fails, clear Vercel build cache and redeploy.
